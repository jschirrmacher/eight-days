# SITE CONFIGURATION
baseurl: "/eight-days"
url: "https://jschirrmacher.github.io"

# THEME-SPECIFIC CONFIGURATION
title: In acht Tagen um die Welt
description: "Reisetagebuch einer virtuellen Weltreise in acht Tagen in Zeiten der COVID-19 Pandemie"
favicon: assets/favicon.ico                             # Icon displayed in the tab

# Header and footer text
header_text: In acht Tagen um die Welt
header_feature_image: assets/img/header.jpg
footer_text: >
  Powered by <a href="https://jekyllrb.com/">Jekyll</a> with <a href="https://github.com/sylhare/Type-on-Strap">Type on Strap</a>

# Blog
excerpt: truncate
post_navigation: true
color_image: /assets/img/lineart.png                    # A bit transparent for color posts.

# Features
katex: false                                            # Enable if using math markup
google_analytics:                                       # Tracking ID, e.g. "UA-000000-01"
disqus_shortname: https-jschirrmacher-github-io-eight-days
layout_variables:                                       # Open the "_sass > base" folder, and open "_variables.scss"
footer_icons:                                           # In _data/social.yml
share_buttons:                                          # In _data/social.yml
authors:                                                # In _data/authors.yml
language:                                               # In _data/language.yml

gallery_path: "gallery"

# PAGINATION
paginate: 5
paginate_path: "blog/page:num"

# PORTFOLIO
#collections:
#  portfolio:
#    output: true
#    permalink: /:collection/:name

# BUILD SETTINGS
markdown: kramdown
highlighter: rouge
sass:
  sass_dir: _sass
  style: :compressed
plugins: [jekyll-paginate, jekyll-seo-tag, jekyll-feed]
exclude: [".jekyll-cache", "Gemfile", "Gemfile.lock", "type-on-strap.gemspec", "*.gem", ".idea", "vendor/*",
          "LICENSE", "README.md", "screenshot.png", "assets/package.json", "assets/node_modules", "assets/gulpfile.js"]

# theme: type-on-strap                                  # if using the theme as a jekyll theme gem
remote_theme: sylhare/Type-on-Strap                     # If using as a remote_theme in github
